usa_psiop = {
    icon = generic_propaganda

    # Категория видна только США
    visible = {
        tag = JAP
        has_country_flag = USA_stierlitz_skin_active
    }
}
ast_psiop = {
    icon = generic_propaganda

    # Категория видна только США
    visible = {
        tag = AST
        has_country_flag = AST_sas_skin_active
    }
}