jap_leader_skins = {
    # Решение: надеть скин Штирлица
    jap_become_stierlitz = {
        icon = generic_formable_nations

        available = {
        }

        visible = {
            always = yes
        }

        ai_will_do = {
            base = 0
        }

        cost = 0
        fire_only_once = yes

    complete_effect = {
    # Устанавливаем флаг, блокирующий повторное появление этого решения
    set_country_flag = JAP_stierlitz_path_active
    # Применяем имя и портрет проверенным методом
    }
    }   
}
usa_leader_skins = {}
ast_leader_skins = {
    ast_become_Pozdniakov = {
        icon = generic_formable_nations

        available = {
            NOT = { has_country_flag = AST_sas_skin_active }
        }

        visible = {
            always = yes
        }

        ai_will_do = {
            base = 0
        }

        cost = 0
        fire_only_once = no

        complete_effect = {
            # Устанавливаем флаг, блокирующий повторное появление этого решения
            set_country_flag = AST_sas_skin_active

            # Применяем имя и портрет проверенным методом
            set_country_leader_name = { 
                name = "Поздняков"
            }
            set_country_leader_portrait = {
                portrait = "gfx/Skini_Dlya_Liderov/Pozdniakov.dds"
            }
        }
    }
}
ita_leader_skins = {
    ita_become_Vaira = {
        icon = generic_formable_nations

        available = {
            NOT = { has_country_flag = ITA_vaira_skin_active }
        }

        visible = {
            always = yes
        }

        ai_will_do = {
            base = 0
        }

        cost = 0
        fire_only_once = no

        complete_effect = {
            # Устанавливаем флаг, блокирующий повторное появление этого решения
            set_country_flag = ITA_vaira_skin_active

            # Применяем имя и портрет проверенным методом
            set_country_leader_name = { 
                name = "Вайра"
            }
            set_country_leader_portrait = {
                portrait = "gfx/Skini_Dlya_Liderov/Vaira.dds"
            }
        }
    } 
}
